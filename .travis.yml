language: php
php:
    - '7.3'
services:
    - postgresql
before_script:
    - cp .env.travis .env
    - mysql -e 'create database forge_test;'
   
install:
    - composer self-update
    - make install
    - php artisan migrate
script:
    - make lint test
    